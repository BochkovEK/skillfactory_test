FROM alpine:3.14
ARG FAVICONS_DIR
ARG FAVICONS_SCRIPT
ENV FAVICONS_DIR $FAVICONS_DIR
ENV FAVICONS_SCRIPT $FAVICONS_SCRIPT
RUN apk add --no-cache wget
RUN mkdir -p $FAVICONS_DIR
COPY $FAVICONS_SCRIPT /
RUN chmod 755 /$FAVICONS_SCRIPT
CMD /${FAVICONS_SCRIPT} ${DOMAIN}
